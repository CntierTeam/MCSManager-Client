display_name: MCSManager Client
short_description: Use mcsm CLI/TUI to operate an MCSManager Panel
default_prompt: >-
  Follow the mcsmanager-cli skill for product usage of `mcsm` (install, config,
  API Key, instances/files/terminal). Prefer CLI + --json. Do not treat this as
  a Rust development guide.
